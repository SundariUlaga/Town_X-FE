# Empty = same-origin via Vite proxy (required for HttpOnly session cookie on localhost).
# Example cross-origin (cookies need Secure + SameSite=None): VITE_API_URL=https://api.example.com
VITE_API_URL=
